import template from './completed.html';
import controller from './completed.controller';

let CompletedComponent = {
  restrict: 'E',
  scope: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default CompletedComponent;